[package]
name = "engish"
version = "0.1.0"
edition = "2021"

[features]
default = ["words", "nouns"]
words = []
nouns = []

[dependencies]
ron = "0.8"
rand = { version ="0.8", features = ["serde1"] }
serde = { version = "1", features = ["derive"] }

[build-dependencies]
ron = "0.8"
rand = { version ="0.8", features = ["serde1"] }
serde = { version = "1", features = ["derive"] }